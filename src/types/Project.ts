export interface Project {
    name: string;            // Project title or internal system name
    description: string;     // Summary of what the system does
    techStack: string[];     // List of technologies used
    role: string;            // Your specific role or contribution
    githubUrl?: string;      // Optional: GitHub link (for open source)
    demoUrl?: string;        // Optional: Live demo URL
  }
  